[package]
name = "memory_engine"
version = "0.1.0"
edition = "2021"
description = "Reusable three-layer memory engine core"

[lib]
name = "memory_engine"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
