# Sessions

Each session should get its own folder.

Do not put all sessions into one growing file. After several sessions, that becomes unreadable and hard to audit.

Expected future shape:

```text
sessions/
  2026-05-17_001/
    session.md
    events.jsonl
    result.md
```

This is a layout rule, not a requirement to create fake sessions before the engine exists.
